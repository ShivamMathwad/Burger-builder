# Burger Builder
A Burger builder application built using React.js and Redux.js. It is a single page application built with proper components and is Mobile Responsive. It uses all the new features introduced in ES6. It also uses proper authentication and form validation, and implements routing uses React Router. Google Firebase is used for data storage.
